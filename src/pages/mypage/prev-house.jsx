import React from "react";
import styles from "styles/mypage/prev-house.module.css";

const PrevHouse = (props) => {
  return (
    <section className={styles[`container`]}>
      <div className={styles[`title`]}>
        <h3>이전 집 기록 보기</h3>
      </div>
      <ul>
        <li></li>
      </ul>
    </section>
  );
};

export default PrevHouse;
